PREFIX schema: <http://schema.org/>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

SELECT ?HealthSocre (COUNT(distinct ?recipe) as ?Count) ((?Count/50) as ?Percentage) WHERE {
    ?node schema:name ?recipe .
  	?node schema:HealthScore ?HealthSocre .
} group by ?HealthSocre